#pragma once
#include <stack>
#include <iostream>
#include "NumberToCharArray.h"


char* Parseinteger(long long int Value);
char* ParseDouble(); //Future implementation.Currently only returns null if called. 
void SafeDelete(char* Pointer);